// declaração tradicional
function soma(x, y) {
    const resultado = x + y;
    return resultado;
}
// declaração com arrow function
const soma1 = (x, y) => {
    const resultado = x + y;
    return resultado;
}

console.log(soma(2, 5));